export enum ActionTypes {
    /** //TODO just an example */
    PLAY_TRACK = 'PLAY_TRACK',
    /** Retrieves the last compilation from the persistent storage, and commits it into the store */
    RETRIEVE_COMPILATION = 'RETRIEVE_COMPILATION',
}
